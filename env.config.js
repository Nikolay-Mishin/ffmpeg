import { log } from 'console';
import { cwd } from 'process';

export const
    root = cwd(),
    envRoot = '.env',
    libRoot = 'lib',
    std = true,
    rewrite = true,
    setRoot = true,
    exclude = ['Git'];
